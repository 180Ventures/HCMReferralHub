export enum Tables {
  users = "users",
  contacts = 'contacts',
  leads = 'leads',
  //delete
  comments = "comments",
  replies = "replies",
  chapters = "chapters",
}

export enum LeadStatus {
  pending = 'Pending',
  sold = 'Sold',
  loss = 'Loss'
}

export enum PriceByStatusLead {
  sold = '250',
  loss = '0'
}